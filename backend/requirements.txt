# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database
sqlalchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.10

# Cache
redis==5.2.0

# Data / ML
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
xgboost==2.1.3
joblib==1.4.2

# Validation / config
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# HTTP client (for NHL API scraping)
httpx==0.27.2

# Observability
prometheus-client==0.21.0
python-json-logger==2.0.7
watchtower==3.3.1           # CloudWatch Logs direct shipping (optional outside k8s)

# AWS
boto3==1.35.0               # S3, Secrets Manager, CloudWatch

# Dev / testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
testcontainers[postgres]==4.8.1   # integration tests with real PostgreSQL

# Retry logic and terminal output
tenacity==9.0.0
rich==13.9.4

# Security
slowapi==0.1.9                     # rate limiting (Starlette/FastAPI)

# AI / LLM
anthropic==0.86.0                  # Claude API (RAG agent + draft summaries)
pgvector==0.4.2                    # pgvector Python client for vector similarity search

# Error tracking
sentry-sdk[fastapi]==2.19.2        # exception monitoring + performance tracing

# ML explainability + drift detection
shap==0.46.0                       # SHAP feature attributions for XGBoost

# Observability: distributed tracing (OpenTelemetry)
opentelemetry-sdk==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
opentelemetry-instrumentation-sqlalchemy==0.50b0
opentelemetry-exporter-otlp-proto-grpc==1.29.0
